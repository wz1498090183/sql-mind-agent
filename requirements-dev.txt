# ============================================================
# SQL Mind Agent 开发依赖（代码质量 & 测试）
# 安装: pip install -r requirements-dev.txt
# ============================================================

# —— 测试 ——
pytest>=8.0.0
pytest-asyncio>=0.24.0

# —— 代码质量 ——
ruff>=0.5.0

# —— 类型检查 ——
mypy>=1.10.0
